<% content_for :title, "Write a reply" %>

<header class="mb-6">
  <%= link_to "← Saved replies", workspace_saved_replies_path(workspace_slug: current_workspace.slug),
        class: "text-sm text-muted underline underline-offset-4 hover:text-heading" %>
  <h1 class="mt-3 font-display text-3xl font-normal sm:text-4xl">Write a reply</h1>
</header>

<%= render "form" %>
